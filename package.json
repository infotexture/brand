{
  "name": "brand",
  "devDependencies": {
    "husky": "0.14.3",
    "lint-staged": "5.0.0",
    "prettier": "1.8.2"
  },
  "lint-staged": {
       "*.{json,md,scss}": ["prettier --write", "git add"]
  },
  "scripts": {
    "fix": "prettier --write \"**/*.{json,md,scss}\"",
    "precommit": "lint-staged"
  }
}
